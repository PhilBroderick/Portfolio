@page
@model Portfolio.Pages.Blog

@{
}

<h1>Blog Posts</h1>
<ul class="blog-list">
    @foreach (var blog in Model.Blogs)
    {
        <li id="@blog.Id">
            <div class="d-flex">
                <p class="w-15">@blog.Created.Date.ToString("dd-MM-yyyy")</p>
                <div class="d-flex flex-column w-85">
                    <h2>@blog.Title</h2>
                    <p>First few paragraphs</p>
                </div>
            </div>
        </li>
    }
</ul>

